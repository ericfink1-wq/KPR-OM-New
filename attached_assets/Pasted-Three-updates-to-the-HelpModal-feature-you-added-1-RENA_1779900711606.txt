Three updates to the HelpModal feature you added:

1. RENAME THE HEADER BUTTON. Change the "?" circular icon button to a text pill labeled "Tutorial". Match the styling of the other header buttons (same height, same olive accent treatment), just wider to fit the word. aria-label="Open tutorial". Click behavior stays the same — opens HelpModal with all sections collapsed.

2. REMOVE THE AUTO-OPEN ON FIRST VISIT. Delete the localStorage check on app mount that auto-opens the modal when 'kpr_help_seen' is absent, and remove the line that writes 'kpr_help_seen' on close. The modal should now only open when someone clicks the Tutorial button. You can remove the kpr_help_seen flag entirely.

3. UPDATE SECTION DETAIL CONTENT to exactly match the wording below. Brief content and section structure stay as-is — only the detail blocks (the expanded "more info" bullets) change. Replace verbatim.

   Also update the modal SUBTITLE from "Click any step to expand more detail." to "Skim the briefs to get going. Click any section for more detail when you want it."

   SECTION 1 — Sign in
   Detail bullets (replace all):
   • Forgot the team password? Ping the admin who shared the site — there's no self-serve reset.
   • Same login works on phone, tablet, and desktop.
   • If you get signed out unexpectedly, nothing's lost — everything lives in the shared database. Just sign back in.

   SECTION 2 — What you see on the home page
   Detail bullets (replace all):
   • **Intelligence Library** stats: OMs Read = how many offering memoranda the AI has processed. Tenant Brands = unique brands across the library (Target counts once even if it shows up at five centers). SF Analyzed = total gross leasable area.
   • **Owned Portfolio** only counts deals tagged Owned. If a deal is missing here that you'd expect, check its status flag on the deal page.
   • **Deal statuses** flow Prospect → Under Contract → Owned → Sold (or Passed). Status drives where the deal shows up on the home page.

   SECTION 3 — Ask the analyst
   Detail bullets (replace all):
   • **Be specific.** "WALT for our owned grocery-anchored centers" gets a real answer; "Tell me about the portfolio" gets a vague one.
   • **Ask for tables and ranked lists** — the analyst will format them. Try "Rank our prospects by cap rate" or "Top 10 tenants by total rent across the portfolio."
   • **Comparisons are a strong use case.** "Compare Vestavia and Pointe Plaza on tenant mix and occupancy" works well.
   • **It only knows what's been uploaded.** If you ask about a property that isn't in the library, it won't know — upload the OM first.
   • **If the numbers look off,** open the source deal — the rent roll might be stale and need a refresh.

   SECTION 4 — Open a deal
   Detail bullets (replace all):
   • **Tenant roster columns:** base rent (not gross), rent/SF, lease commencement and expiration, remaining term keyed to the as-of date, and an anchor flag for tenants ≥ 10,000 SF or branded as such.
   • **Vacant suites** show up as "Vacant" rows with their square footage — they keep occupancy math honest.
   • **The "as of" date** at the top tells you how stale the data is. If it's months old, refresh the roster (see Section 5).
   • **For owned deals,** you'll also see Deal Terms (purchase price, going-in cap, closing date, seller, broker) and Financing (lender, loan amount, rate, maturity). Click the edit pencil to update any field.
   • **Cover photo wrong?** Use "Set cover from page #" to pick a different page from the OM. Same for the site plan.

   SECTION 5 — Refresh or add deals (admin)
   Detail bullets (replace all):
   • **Why the upload password exists:** OM extraction and rent-roll AI refresh hit the Anthropic API and cost real money per run. Keeping that password tight means everyone can browse the library and chat with the analyst freely, but only a couple of people can spend.
   • **OM upload flow:** drop a PDF on the home page, the extractor runs for 1–3 minutes, the deal appears with cover photo and site plan auto-picked. If data looks wrong, use "Re-run extraction" on the deal page rather than re-uploading.
   • **The free rent-roll path in detail:** open Claude (the regular chat app, not the API) and send the rent roll PDF. Ask for it in the site's roster schema — Claude returns a JSON block. Copy it, open the "Paste roster from Claude" toggle, paste, and click Apply. Same result as the PDF refresh, zero tokens spent.
   • **Backup vs Restore:** Backup downloads a full JSON snapshot. Restore *merges* deals back in by ID — it does NOT wipe what's there. Do a Backup before any big import or batch edit.

Don't touch anything else — the modal structure, the expand/collapse behavior, the rotating chevrons, the styling, the brief content of each section, and Escape/backdrop close all stay exactly as they are. This is the only Agent run needed; do all three updates together.
