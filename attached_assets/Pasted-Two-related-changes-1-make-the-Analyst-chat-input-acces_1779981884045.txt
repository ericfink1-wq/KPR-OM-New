Two related changes: (1) make the Analyst chat input accessible from almost everywhere, and (2) fix the floating feedback button's vertical position based on whether a bottom bar is present.

CONTEXT (src/App.tsx): the app has tabs (analyst, portfolio, analytics, comps) and a `view` state whose view.type is one of "list","detail","compare","tenant","tenant-audit","lender". The Analyst tab renders the full <AnalystChat> with its own pinned input bar. There is an existing handleQuery(q: string) that sets pendingQuery and switches to the Analyst tab, and AnalystChat auto-sends the initialQuery — this is what the property detail page's "ask about this property" already uses. Reuse handleQuery; do not build a second chat engine.

PART 1 — Global Analyst ask-bar:
1. Create src/components/AnalystBar.tsx: a slim chat input bar that visually matches the input at the bottom of AnalystChat — same placeholder "Ask anything about your deal library…", same green circular ↑ send button, same helper subtext "Enter to send · Shift+Enter for new line · Powered by Claude", same cream/Inter styling. Prop: onAsk: (q: string) => void. On Enter (no Shift) or send click, if text is non-empty, call onAsk(text) and clear. Make it flex-shrink:0, full width, non-scrolling.
2. In src/App.tsx render <AnalystBar onAsk={handleQuery} /> pinned to the bottom of the main content column, shown on every tab/view EXCEPT: (a) the Analyst tab (tab === "analyst"), and (b) the individual property detail page (view.type === "detail"). Show condition: tab !== "analyst" && view.type !== "detail". So it appears on Portfolio (list), Analytics, Comps, and the compare/tenant/lender/tenant-audit views.
3. Place it as a flex-shrink:0 sibling pinned to the bottom of the existing top-level flex column (not overlapping the right-side help panel padding). Ensure scrollable content above has ~88px bottom clearance so nothing hides behind it (the main tab scroll containers already use paddingBottom:80 — bump to ~88; add equivalent bottom padding to any full-height view like compare where content would sit under the bar).

PART 2 — Feedback button position:
4. The feedback button (src/components/FeedbackWidget.tsx) is currently at bottom:90 everywhere, which sits too high on pages with no bottom bar. Add a boolean prop liftAboveBar: when true use bottom:90 (clears the bar), when false use bottom:24 (normal low position). Keep left:24 and keep the popover anchored correctly at both heights.
5. In src/App.tsx pass liftAboveBar={view.type !== "detail"} — after Part 1 every view has a bottom bar except the property detail page.

Do not change the Analyst tab's existing full chat or other unrelated behavior. Make, test, iterate until: a slim Analyst ask-bar appears at the bottom of Portfolio/Analytics/Comps and the tenant/lender/compare/tenant-audit views and typing there jumps into the Analyst with the question sent; it does NOT appear on the Analyst tab or an individual property page; no content is hidden behind it; and the feedback button sits just above the bar everywhere except the property page, where it sits low.
