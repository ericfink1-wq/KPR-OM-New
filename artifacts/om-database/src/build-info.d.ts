// Build-time constants injected by Vite's `define` (see vite.config.ts). Replaced
// with string literals at build time; on a non-Vite path they may be undefined, so
// readers must guard. Used by the on-screen version stamp.
declare const __BUILD_ID__: string | undefined;
declare const __BUILD_TIME__: string | undefined;
